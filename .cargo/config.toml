[target.aarch64-apple-darwin]
# Link with LLVM lld instead of Apple ld. It is much faster when many small
# test binaries link, which is the main per-mutant cost on this workspace.
# Install with: brew install lld
rustflags = ["-C", "link-arg=--ld-path=/opt/homebrew/bin/ld64.lld"]
